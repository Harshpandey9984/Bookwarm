{
  "version": 2,
  "buildCommand": "cp package.production.json package.json && npm install --only=production && npm run build",
  "outputDirectory": "build",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "env": {
    "GENERATE_SOURCEMAP": "false"
  }
}
